#include <stdio.h>

/**
 * main - prints its name, folowed by a new name
 * @argv: argument vector
 * @argc: argument count
 *
 * Return: 0.
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
